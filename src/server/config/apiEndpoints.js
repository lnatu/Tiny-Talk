const apiVersion = '/api/v1';

const apiEndpoints = {
  users: `${apiVersion}/users`,
  conversations: `${apiVersion}/conversations`,
  messages: `${apiVersion}/messages`
};

module.exports = apiEndpoints;
